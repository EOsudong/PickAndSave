# 📱 PickAndSave - 프론트엔드 UI/UX 기획 및 설계서

## 1. 개요 (Overview)
* **목적**: 사용자가 쿠팡 상품의 가격 변동 추이를 직관적으로 확인하고, 최저가 시점에 구매 및 알림을 받을 수 있도록 돕는 웹앱
* **핵심 목표**: 검색엔진 유입(SEO) 극대화, 빠른 가격 판단 지원, 쿠팡 파트너스 구매 링크 전환율 향상
* **권장 기술 스택**:
    * **Framework**: Next.js (App Router, SSR/SSG 활용)
    * **Styling**: Tailwind CSS, shadcn/ui
    * **State & Data**: TanStack Query (React Query)
    * **Chart**: Recharts
    * **Icons**: Lucide React

---

## 2. 화면별 상세 UI/UX 스펙

### 2.1 메인 홈 화면 (`/`)
* **상단 헤더**:
    * 로고, 관심 상품 탭, 알림 센터 버튼, 로그인/프로필 버튼
* **통합 검색바 (Hero Section)**:
    * "상품명 검색 또는 쿠팡 URL을 붙여넣으세요" 입력을 받을 수 있는 대형 검색창
    * 쿠팡 URL 입력 시 즉시 해당 상품 정보를 파싱하여 상세 페이지로 이동
* **오늘의 역대 최저가 (핫딜 섹션)**:
    * Grid 형태의 상품 카드 배치
    * **카드 구성**: 상품 이미지, 브랜드명, 상품명, 현재가, 역대 최저가 비교 배지(`-15%`, `역대 최저가`), 로켓배송 아이콘
* **카테고리 퀵 메뉴**:
    * 전자제품, 생활용품, 패션 등 주요 카테고리 칩(Chip) 형태 아이콘

---

### 2.2 상품 상세 페이지 (`/products/[id]`)
* **가격 상태 인디케이터 (신호등 UX)**:
    * 상단에 구매 적정성을 한눈에 보여주는 상태 바 제공
        * 🟢 **지금이 구매 적기!** (역대 최저가 도달 또는 평소 대비 X% 할인)
        * 🟡 **보통 가격** (평균가 유지 중)
        * 🔴 **비싼 편** (최근 가격 상승)
* **대형 가격 추이 그래프**:
    * Recharts 기반 Line Chart
    * **기간 선택 버튼**: `7일`, `30일`, `90일`, `1년`, `전체`
    * 그래프 상에 **'역대 최저가'** 및 **'역대 최고가'** 지점 마커 표시
* **가격 정보 요약 카드**:
    * 현재가, 역대 최저가, 역대 최고가, 평균가를 2x2 Grid로 표현
* **고정 하단 CTA 바 (Sticky Bottom Bar)**:
    * 모바일 화면 스크롤 시에도 하단에 고정되는 결제 유도 바
    * **좌측**: 관심 상품 하트($\heartsuit$) 버튼, 알림 설정 종($\Delta$) 버튼
    * **우측**: `쿠팡에서 최저가로 구매하기` 대형 버튼 (클릭 시 파트너스 링크로 이동)

---

### 2.3 관심 상품 & 알림 설정 모달
* **관심 상품 목록 (`/my/products`)**:
    * 유저가 등록한 상품들의 현재가 변동 상황을 한눈에 보는 리스트 View
    * 등록 당시 가격 대비 상승/하락 폭 표시
* **알림 설정 모달 (Modal)**:
    * **목표 가격 설정**: 직접 금액 입력 또는 "현재가 대비 5%, 10% 하락 시" 슬라이더 조절
    * **알림 수단 선택**: 웹 푸시(Web Push), 이메일, 카카오톡 알림톡 체크박스

---

## 3. 핵심 UX 디자인 규칙

### 3.1 시각적 피드백 & 컬러 시스템
* **가격 하락 (유저에게 이득)**: 🟢 **Green** (`#10B981`) 또는 🔴 **Red** (`#EF4444`, 한국 주식/커머스 기준 상승/하락 선호에 맞춰 통일)
* **로켓배송 / 쿠팡 요소**: 🔵 **Blue** 계열 강조 색상
* **역대 최저가 배지**: 반짝이는 애니메이션 효과(Pulse) 적용하여 시선 집중

### 3.2 반응형 및 모바일 퍼스트 (Mobile-First)
* 유저 유입의 80% 이상이 모바일 기기이므로, 모바일 웹 터치 영역(최소 48px)과 한 손 조작성을 최우선 고려
* 하단 탭 바(Bottom Navigation Bar) 제공

---

## 4. 프론트엔드 폴더 구조 예시 (Next.js App Router)

```text
src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx                  # 메인 홈 화면
│   ├── products/
│   │   └── [id]/
│   │       └── page.tsx          # 상품 상세 페이지
│   └── my/
│       └── products/
│           └── page.tsx          # 관심 상품 목록
├── components/
│   ├── common/                   # Header, Footer, Navigation
│   ├── ui/                       # Button, Modal, Badge (shadcn)
│   ├── product/                  # ProductCard, PriceChart, PriceIndicator
│   └── search/                   # SearchBar
├── hooks/                        # usePriceHistory, useAlert
└── lib/                          # utils, api-client